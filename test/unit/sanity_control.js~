'use strict';

describe('controllers',function(){

	beforeEach(module('toolboxControllers'));

	it('exist',inject(function($controller,$modal){
		var scope={};
		var ctrl=$controller('IndexCtrl',{$scope:scope});
		expect(scope.currentUser).equals(null);
	}));


});
